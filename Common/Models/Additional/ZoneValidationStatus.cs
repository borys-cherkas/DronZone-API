namespace Common.Models.Additional
{
    public enum ZoneValidationStatus
    {
        WaitingForAdministrator = 100,
        InProgress = 200,
        Confirmed = 300,
        Declined = 400
    }
}
